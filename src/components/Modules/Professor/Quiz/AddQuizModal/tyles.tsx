export type initialStateType = {
    quizName: string,
    faculty: string,
    timer: boolean,
    showAnswers: false,
    questions: {}[],
    subjects: string,
    quizEditing: boolean,
    quizEditingId?: string,
}